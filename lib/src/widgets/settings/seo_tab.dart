import 'package:flutter/material.dart';

class SeoSettingsTab extends StatelessWidget {
  const SeoSettingsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
        "IDK how to do the SEO stuff on flutter web dynamically yet");
  }
}
