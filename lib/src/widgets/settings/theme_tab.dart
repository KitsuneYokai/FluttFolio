import 'package:flutter/material.dart';

class ThemeSettingsTab extends StatelessWidget {
  const ThemeSettingsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Theme Settings");
  }
}
